'use strict';

/**
 * recommended-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::recommended-page.recommended-page');
